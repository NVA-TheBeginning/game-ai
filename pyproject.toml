[project]
name = "game-ai"
version = "0.1.0"
description = "todo"
readme = "README.md"
requires-python = ">=3.14.0"
dependencies = [
    "pickledb>=1.3.2",
    "websockets>=15.0.1",
]

[dependency-groups]
dev = ["pytest>=8.4.2", "ruff>=0.14.4", "ty>=0.0.1a25"]
